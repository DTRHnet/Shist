{
  "version": 2,
  "buildCommand": "npm run vercel:build && npm run postdeploy",
  "outputDirectory": "dist/public",
  "regions": ["iad1", "sfo1"],
  "functions": {
    "dist/index.js": {
      "memory": 1024,
      "maxDuration": 10
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "dist/index.js" },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "dist/public/index.html" }
  ],
  "crons": [
    {
      "path": "/api/invitations/cleanup",
      "schedule": "0 4 * * *"
    }
  ]
}
